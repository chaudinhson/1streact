import React from "react";
import ReactDOM from "react";
import Game from "../src/gamecomponent.js";
const root = ReactDOM.createRoot(document.getElementById("Gameroot"));
root.render(
  <React.StrictMode>
    <Game />
  </React.StrictMode>
);
